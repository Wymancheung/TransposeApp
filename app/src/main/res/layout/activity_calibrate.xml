<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">


    <ScrollView
        android:id="@+id/scrollview_image_cali"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        android:fillViewport="true">

        <ImageView
            android:id="@+id/imageView_cali"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:src="@drawable/camera" />

    </ScrollView>

    <ScrollView
        android:id="@+id/scrollview_text_cali"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/scrollview_image_cali"
        android:fillViewport="true">

        <EditText
            android:id="@+id/editText_cali"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:ems="10"
            android:inputType="textMultiLine"
            android:paddingBottom="70dp" />

    </ScrollView>

    <LinearLayout
        android:id="@+id/linear_cali"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <Button
            android:id="@+id/refresh_cali"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:text="Refresh" />

        <Button
            android:id="@+id/confirm_cali"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:text="OK" />

    </LinearLayout>

</RelativeLayout>